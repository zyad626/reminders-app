export interface TaskType {
  taskName: string;
  taskStatus: string;
  taskEndTime: string;
  taskDate: string;
  taskColor: string;
  // calculatingRemainingTime: (taskEndDate: string, taskEndTime: string) => string;
}
